Parse()
문자열을 객제로 변환하는 정적메소드
예를 들어 파일에 "점심식사,8000" 같은 문자열을 읽어오면.
이를 객체로 복원해야 실제 프로그램에서 사용할 수 있음

왜 사용하는가?
 - 파일에서 읽은 데이터 => 객체로 변환 : StreamReader로 읽은 문자열을 다시 객체로 복원하기 위해
 - ToString()과 짝을 이룸 : ToString()은 저장, Parse()는 복원 역할
 - 코드 중복 방지 : Split, int.Parse 등 매번 직접하지 않고, 메서드로 분리

 관계도
 객체 -> 문자열 : ToString()
 문자열 -> 객체 : Parse()
